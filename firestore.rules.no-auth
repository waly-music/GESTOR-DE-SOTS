rules_version = '2';
// Solo para desarrollo con VITE_DISABLE_AUTH=true (sin login).
// Cualquiera con la API key puede leer/escribir. NO usar en producción.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
